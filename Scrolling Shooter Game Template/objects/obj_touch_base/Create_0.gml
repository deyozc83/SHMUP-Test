/// Base touch UI object
// Used as a parent to all other touch ui objects

// Touch id unset
touch_id = -1;

// Set the UI buttons to have faded alpha
image_alpha = 0.75;

// Empty touch action function to be updated later
touch_action = function()
{
	
}